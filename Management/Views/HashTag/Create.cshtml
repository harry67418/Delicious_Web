@model Management.Models.Thashtag

@{
    ViewData["Title"] = "新增HashTag";
}

<h1>新增HashTag</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Hasgtag" class="control-label"></label>
                <input asp-for="Hasgtag" class="form-control" />
                <span asp-validation-for="Hasgtag" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="新增HashTag" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
