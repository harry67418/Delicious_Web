@model IEnumerable<prjDelicious.Models.TcollectionFolder>
@using Microsoft.AspNetCore.Http
@using prjDelicious.ViewModel

@*@{
    ViewData["Title"] = "FolderModal";
    Layout = "_Layout_NoSession";
    var login_id = Context.Session.GetString(CDictionary.CURRENT_LOGINED_USERID);
    var member_id = Convert.ToInt32(Context.Request.Query["member"]);
    var folder_id = Context.Request.Query["folder"];
}
@section Style{
    <link href="/assets/css/folder.css" rel="stylesheet">
}*@

<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="staticBackdropLabel">Modal title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                ...
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Understood</button>
            </div>
        </div>
    </div>
</div>